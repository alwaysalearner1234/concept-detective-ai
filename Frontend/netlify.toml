[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback: serve index.html for all routes (Next.js handles routing)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
